# TinyOS v4.0 功能验证与提交计划

## 背景

`recovery/v4.0` 分支已有基础 v4.0 功能（HTTP 客户端、fork/exec、Shell 管道、FAT16 子目录、syscall 27 等），
但 `backup/v4.0-untracked` 分支还有更多未提交的新文件需要迁移过来：

- **VFS 虚拟文件系统层** (vfs.c/vfs.h, DevFS: /dev/null, /dev/zero)
- **MBR 分区解析** (mbr.c/mbr.h)
- **信号系统** (signal.c/signal.h, SIGHUP/SIGINT/SIGKILL/SIGTERM)
- **16+ 新用户程序** (ls/cat/more/ping/arp/net/netstat/dhcp/pci/write/rm/mkdir/rmdir/dino/si/tinyhttpd/filetest/kill)
- **Shell 增强** (后台任务 `&`, `jobs`, `sh` 脚本执行)
- **Ctrl+C 中断支持** (SIGINT 投递)
- **调度器修复** (per-task kernel_exit_esp)
- **Python 测试框架** (tests/ 目录)
- **libc 扩展集成** (errno.c/termios.c 纳入构建)

## 原则

1. 每步从 backup 分支 checkout 相关文件
2. 做必要的内核集成修改（shell.c, interrupts.c, Makefile 等）
3. `make` 构建通过
4. 运行 QEMU 自动/手动测试
5. 更新相关文档 (AGENTS.md, ROADMAP.md, USAGE.md)
6. `git commit` 提交

## 分组计划

### Step 1: 测试框架 + 构建基础设施 [DONE b2b145a]
**文件**: `tests/common.py`, `tests/test_features.py`, `tests/cases/*.py`, `user/build.sh`
**验证**: `python tests/test_features.py --list-tests` 列出测试项
**文档**: 无

### Step 2: MBR 分区解析 [DONE 8150472]
**文件**: `kernel/mbr.c`, `include/mbr.h`
**集成**: kernel.c 初始化调用 `mbr_init()`, Makefile 添加 mbr.o
**验证**: QEMU 启动串口日志包含 `[MBR]` 分区信息; `partitions` 命令
**文档**: AGENTS.md 关键文件表, ROADMAP 3.2

### Step 3: VFS 虚拟文件系统 + DevFS [DONE 6d9c25c]
**文件**: `kernel/vfs.c`, `include/vfs.h`
**集成**: kernel.c 初始化 `vfs_init()` + `vfs_register_backend(&devfs_ops)`, interrupts.c 添加 syscall 28-31/62 分发
**验证**: `filetest` 命令, `cat /dev/null`, `cat /dev/zero`
**文档**: AGENTS.md 关键文件, ROADMAP 3.3 VFS, USAGE.md filetest 用法

### Step 4: 信号系统 + Ctrl+C [DONE 7db3cbc]
**文件**: `kernel/signal.c`, `include/signal.h`
**集成**: keyboard.c Ctrl+C 检测, scheduler.c 信号投递, scheduler.h sig_pending/sig_handlers 字段, interrupts.c kill syscall(33), kernel.c signal_init()
**验证**: 运行长任务(schedtest)时按 Ctrl+C 能终止任务
**文档**: AGENTS.md 注意事项, ROADMAP 2.1 信号

### Step 5: 调度器修复 (per-task kernel_exit_esp) [CANCELLED - backup 无差异]
**文件**: `kernel/scheduler.c`, `include/scheduler.h`, `kernel/user.asm`, `drivers/interrupts.asm`
**验证**: `forktest` 不再触发 Page Fault, `schedtest` 正常
**文档**: TROUBLESHOOTING.md (若 forktest page fault 已修复)

### Step 6: FAT16 写入修复 [CANCELLED - backup 无差异]

### Step 7: libc 扩展集成到构建 [DONE 591d49d]
**文件**: `user/build.bat` 修改
**集成**: build.bat 添加 `-I include`, 编译 errno.c/termios.c 并链接到 LIBC
**验证**: `make user-programs` 构建通过
**文档**: AGENTS.md 用户态 libc 章节

### Step 8: 新用户程序 (第一批 - 文件系统类) [DONE 4d012d6]
**文件**: `user/apps/ls.c`, `user/apps/cat.c`, `user/apps/more.c`, `user/apps/write.c`, `user/apps/rm.c`, `user/apps/mkdir.c`, `user/apps/rmdir.c` + 对应 `kernel/embedded_*.asm`
**集成**: Makefile 添加规则, user/build.bat 添加编译, shell.c 命令注册, loader.c run_ 函数, loader.h 声明, help.c 更新
**验证**: 逐个运行 ls/cat/more/write/rm/mkdir/rmdir
**文档**: USAGE.md 新命令说明, AGENTS.md 嵌入式 ELF 列表

### Step 9: 新用户程序 (第二批 - 网络类) [DONE 21143ea]
**文件**: `user/apps/ping.c`, `user/apps/arp.c`, `user/apps/net.c`, `user/apps/netstat.c`, `user/apps/dhcp.c` + 对应 embedded_*.asm
**集成**: 同 Step 8 模式
**验证**: `ping 10.0.2.2`, `arp`, `net`, `netstat`, `dhcp`
**文档**: USAGE.md 网络命令更新

### Step 10: 新用户程序 (第三批 - 系统/工具类) [DONE 9df6a3b]
**文件**: `user/apps/pci.c`, `user/apps/kill.c`, `user/apps/filetest.c` + embedded_*.asm
**集成**: 同上
**验证**: `pci`, `kill`, `filetest`
**文档**: USAGE.md 更新

### Step 11: 新用户程序 (第四批 - 游戏/服务器) [DONE 9df6a3b]
**文件**: `user/apps/dino.c`, `user/apps/si.c`, `user/apps/tinyhttpd.c` + embedded_*.asm
**集成**: 同上
**验证**: `dino`, `si`, `tinyhttpd`
**文档**: USAGE.md 更新

### Step 12: Shell 增强 (后台任务, jobs, sh 脚本) [CANCELLED - backup 无源码差异]
**文件**: `kernel/shell.c` 修改, `user/apps/help.c` 修改
**验证**: `echo hello &`, `jobs`, 写脚本文件后 `sh script.txt`
**文档**: USAGE.md Shell 增强说明, ROADMAP 4.2

### Step 13: execve 系统调用 [CANCELLED - backup 无源码差异]
**文件**: `kernel/loader.c`, `include/loader.h`, `drivers/interrupts.c`
**验证**: execve 从 VFS 加载 ELF 替换当前程序
**文档**: ROADMAP 4.1, ARCHITECTURE.md

### Step 14: 文档全面同步 + 测试验证 [DONE]
**文件**: 所有 docs/*.md, AGENTS.md
**验证**: 构建通过，文档同步
**提交**: 文档更新提交

## 执行结果

- **Step 1-3**: 已完成 (b2b145a, 8150472, 6d9c25c)
- **Step 4**: 信号系统 (7db3cbc) — 含 Ctrl+C 广播修复
- **Step 5-6**: 跳过 (backup 无差异)
- **Step 7**: libc 集成 (591d49d)
- **Step 8**: 文件系统命令 7 个 (4d012d6) — 含 include 顺序修复
- **Step 9**: 网络命令 5 个 (21143ea)
- **Step 10-11**: 系统/游戏/服务器命令 6 个 (9df6a3b)
- **Step 12-13**: 跳过 (backup 无源码差异)
- **Step 14**: 文档同步完成

**最终状态**: 29 个 Ring 3 用户程序嵌入内核，信号系统 + VFS + MBR 分区全部集成完毕。
